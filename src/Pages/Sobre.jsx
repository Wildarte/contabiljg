export default function Sobre() {
  return (
    <main className="page">
      <header className="hero">
        <p className="eyebrow">Sobre</p>
        <h1>Uma nova pagina</h1>
        <p className="lead">
          Crie futuras paginas dentro de `src/Pages/` e adicione novas rotas no `App.jsx`.
        </p>
      </header>
    </main>
  );
}
